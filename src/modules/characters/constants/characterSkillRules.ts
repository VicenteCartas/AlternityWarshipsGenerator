import type { CharacterSkillRules } from '../types/character';

export const STANDARD_CHARACTER_SKILL_RULES: CharacterSkillRules = {
  startingSkillAllocation: 'standard',
  specialtySkillCosts: 'standard',
};