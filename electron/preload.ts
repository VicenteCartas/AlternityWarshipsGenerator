// Preload script for exposing safe APIs to the renderer process
// Add any IPC handlers here if needed in the future

window.addEventListener('DOMContentLoaded', () => {
  console.log('Alternity Warship Generator loaded');
});
